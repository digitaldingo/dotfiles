# Personal resources

One place for all configuration. One place to rule it all.

## Installation
Clone and add symlinks:

    ln -s ~/resources/vim ~/.vim
    ln -s ~/resources/vimrc ~/.vimrc
    ln -s ~/resources/gvimrc ~/.gvimrc
